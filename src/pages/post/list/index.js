'use strict';

import { test } from '../../ajax/post';

/**
 * @param {string} title
 * @returns {
 *  {
 *      posts: {
 *          id: string,
 *          title: string,
 *          description: string,
 *          createdAt: string,
 *          updatedAt: string
 *      }[]
 *  }
 * }
 */
// async function fetchPostList(title) {
//   const response = await fetch;
// }

console.log(test());
